#include <bits/stdc++.h>

using namespace std;

int main() {
    
  // 1. For integers values.

    // int - for certain ranges like for not all big numbers.
    int x = 13; // valid variable name
    // long - can be use to store big numbers. 
    long y = 1231;
    // long long - if you need even wider range to store your numbers long long can help.
    long long u = 1312414124;

  // 2. For Float/Decimal values.

    //float - can store decimal values and integer values to certain range.
    float g = 234.43; 
    float a = 2;
    //double - can store decimal values as well as integer values for larger ranges.

  
  // 3. For strings and characters.

    // U can use char ,string and getline datatypes for it.

    //strings - can store bunch of characters until it finds a space.
    string s = "Divyam Sirswal";

    //getline - can store complete characters including spaces and works until the line breaks (enter press).
    string l;
    //getline(cin,l);

    //char - can store a single character under single quotes.
    char ch = 'd';

    return 0;

}